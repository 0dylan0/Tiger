@model Web.Models.UserModel
@{
    ViewBag.Title = "添加用户信息";
}

@using (Html.BeginForm("Add", "Users", FormMethod.Post, new { id = "Add", data_val = true }))
{
    @Html.AntiForgeryToken()
    <div class="box box-solid">
        <div class="box-body">

            <div class="row">

                <div class="col-xs-12 col-sm-12 col-md-8">
                    <div class="form-group">
                        @Html.LabelFor(m => m.Name, new { @class = "control-label" })@RequiredTag()
                        @Html.TextBoxFor(m => m.Name, new { @class = "form-control " })
                        @Html.ValidationMessageFor(m => m.Name)
                    </div>
                </div>
            </div>
            <div class="row">

                <div class="col-xs-12 col-sm-12 col-md-8">
                    <div class="form-group">
                        @Html.LabelFor(m => m.Password, new { @class = "control-label" })@RequiredTag()
                        @Html.TextBoxFor(m => m.Password, new { @class = "form-control " })
                        @Html.ValidationMessageFor(m => m.Password)
                    </div>
                </div>
            </div>
        </div>

        <div class="box-footer">
            <button id="submitChange" type="submit" class="btn btn-primary"><i class="fa fa-save"></i> 保存</button>
            <a href="@Url.Action("Index")" class="btn btn-default"><i class="fa fa-undo"></i> 返回</a>
        </div>
    </div>
}
@helper RequiredTag()
{
    @Html.Partial("_RequiredTagPartial")
}



@section scripts{


    <script type="text/javascript">

        //$.validator.addMethod("aaaDate", function (value, element, param) {

        //    var reg = /^[\u4e00-\u9fa5]$/;
        //    return reg.test(userName.value) != true

        //}, "用户应为全中文");

        $(function () {
            //$("form").valid();
            //$("#Name").rules("add", {
            //    NameData: true,

            //});

        });
    </script>
}





